# 유튜브 업로드·다국어 툴체인 (W9에서 검증, 2026-07-09)

> 이번에 **실제로 통한 파이선 스크립트들**만 순서대로. 다음엔 이대로만 가면 헤매지 않는다.
> 대상: 영상별 KO/EN 두 파일 → 각각 일부공개 업로드 + 5개국어(한/영/일/중/스) 자막·제목설명.

---

## 준비물 (업로드 전)
- **영상**: `hangeul_..._np_ko.mp4` / `_en.mp4` (4K)
- **자막(원본)**: `..._np.ko.srt` / `..._np.en.srt`
- **다국어 자막**: `w9pkg/<vid>_<lang>.srt` — agy(조감독)로 번역 후 **원본 타임스탬프로 rebuild**(한국어 단어·타임스탬프 유지)
- **제목/설명**: `w9pkg/<vid>_title.txt` / `_desc.txt` (챕터 타임라인 포함) + 다국어 `<vid>_<lang>_title.txt` / `_desc.txt` (agy 번역)
- **썸네일**: 전용 이미지(agy) + PIL 텍스트, 1280×720 <2MB

---

## ★★★ 한 번에 (오케스트레이터 `yt_localize.py`) — 다음엔 우물거리지 말고 이걸로
영상 업로드(upload_cdp) 후, **매니페스트 하나로 본언어 설정 + 5개국어 자막·제목설명을 순서대로** 돌린다(크롬종료 자동, 요약표 출력).
```bash
python yt_localize.py <VID> <manifest.json>
#  예: python yt_localize.py Qlr6VRthasw hangeul_birth_vowels/w9pkg/w9_ko_manifest.json
#  --subs-only / --meta-only 로 한쪽만. 2패스=실패 요약 보고 빈 언어만 남긴 매니페스트로 재실행.
```
매니페스트: `{"video_lang":"영어"(선택), "langs":[{"row","srt","title","desc","add","do":["sub","meta"]}]}`.
예제 = `hangeul_birth_vowels/w9pkg/w9_ko_manifest.json` · `w9_en_manifest.json`.
→ 아래 Phase 1·2는 이 오케스트레이터가 내부에서 쓰는 개별 도구(수동/디버그용).

## Phase 1 · 본영상 업로드 (영상별 1회, CDP=로그인된 디버그 크롬 9222)
```bash
# 1) 영상 파일 업로드(일부공개) + 썸네일 + 제목/설명 + 아동용아님 + AI예 + 교육 + VIDEO_ID 캡처
python upload_cdp.py <video.mp4> <desc.txt> "<title>" <thumb.png> unlisted
#    → 로그 VIDEO_ID_FINAL=xxxx 확보. (디버그 크롬 페이지 오염되면 [-1]가 엉뚱한 페이지 잡으니 잉여탭 정리)

# 2) 본영상 언어 설정(안 하면 채널기본=한국어로 잡힘 → EN영상은 꼭 영어로)
python set_video_lang.py <VID> 영어      # KO영상은 한국어(기본이면 생략 가능)
```

## Phase 2 · 다국어 자막·제목설명 (영상별·언어별)
> ★★ 이 스크립트들은 **자체 크롬(af.PROFILE)** — 디버그 크롬(9222)이 같은 프로필을 점유하므로
> **매 실행 전 크롬 종료 필수**:
> ```bash
> K(){ powershell -c "Get-Process chrome -EA SilentlyContinue | Stop-Process -Force -EA SilentlyContinue"; sleep 4; }
> ```
```bash
# 자막 업로드+게시 (본언어=행텍스트만 / 다국어=끝에 add)
K; python tx_sub.py <VID> "한국어 (동영상 언어)" <ko.srt>          # 본언어(동영상 언어) — add 없음
K; python tx_sub.py <VID> "일본어"              <ja.srt> add       # 다국어 — add
#   행텍스트: 한국어 (동영상 언어) / 영어 / 일본어 / 중국어(중국) / 스페인어
#   성공로그: RESULT upload=True publish=True

# 제목·설명 번역 게시
K; python tx_meta.py <VID> "일본어" "$(cat <ja_title.txt>)" <ja_desc.txt>
#   성공로그: === 일본어: 제목=True 설명=True 게시=OK ===
```

## ★ 처음 시도할 때 (권장 순서 — 이대로 하면 대부분 한 번에)
1. **두 영상 업로드**: `upload_cdp.py`로 KO·EN 각각 일부공개 업로드 → VIDEO_ID 확보(처리 대기 불필요).
2. **본언어 설정**: `set_video_lang.py <EN_VID> 영어` (KO영상은 한국어 기본).
3. **영상별 자막 배치**(먼저): `K; tx_sub`로 5개 언어 — 본언어(add 없음) + 다국어 4개(add). *자막이 언어행을 persist시킴 → 먼저 하는 게 안전.*
4. **영상별 제목설명 배치**(그다음): `K; tx_meta`로 5개 언어.
- 우선순위 언어: 한국어(본)·영어·일본어·중국어(중국)·스페인어.
- **첫 도구 선택**: 자막=**tx_sub.py**(cap_robust 아님) / 제목설명=**tx_meta.py** / 파일업로드=**upload_cdp.py**. 처음부터 이 셋으로.

## ★★ 언어가 사라지지 않게 (핵심 원칙)
- **언어추가하면 자막·제목설명 중 "하나라도" 반드시 게시**해야 그 언어행이 남는다(둘 다 안 넣으면 곧 사라짐).
- 그래서 **1패스**: 언어별로 자막 시도 → (실패해도) 제목설명 시도 → **둘 중 하나만 붙어도 언어는 persist**. 한 언어당 두 작업을 다 시도해서 최소 하나는 남긴다.
- **2패스**: 모든 언어가 추가+하나라도 올라온 상태가 되면 → **스크린샷 찍어** 언어별로 뭐가 빈칸("–")인지 보고 → **무엇을 할지 다시 정해** 빈 것만 새로 채운다.

## ★ 모두 성공 못했을 때 (부분 실패 복구 — 무작정 재시도 금지)
1. **재시도 전 스크린샷**: `/{VID}/translations` 페이지를 자체 크롬으로 열어 스크린샷 + 행별 `자막 | 제목설명` 상태 추출.
2. **없는 것만 파악**: 언어별로 자막이 "–"인지, 제목설명이 "–"인지 표로 정리. (언어행이 있으면 persist된 것 — add 없이 빈칸만 채우면 됨)
3. **골라서 재시도**: 자막 없음→`tx_sub`(행 있으면 add 없이), 제목설명 없음→`tx_meta`. 이미 `게시됨`인 건 절대 안 건드림.
4. **잘 실패하는 케이스**:
   - **중국어**: 행이름 `중국어(중국)`(간체 아님). 언어추가는 전체이름 클릭·자막칸은 부분매칭 폴백(tx_sub 수정됨).
   - **EN 한국어 자막**: 본언어가 한국어로 잡혀 행이 `한국어 (동영상 언어)` → **add 없이** tx_sub.
   - **filechooser 타임아웃**: 크롬 종료(K) 확실히 하고 재시도(프로필 잠금·오염 페이지 원인).
5. **다시 스크린샷** → 전부 `게시됨|게시됨` 될 때까지 3~4 반복.

## 방법 원칙 (사장님 확정)
1. **스크린샷으로 상태 파악 → 계획 → 실행** (무작정 재시도 금지).
2. **언어추가만 하고 자막·제목설명 안 넣으면 언어행이 사라진다** → 추가하면 반드시 내용 넣어 게시.
3. 실패한 것만 골라 재시도.

## ⚠️ 함정
- **중국어 행이름 = `중국어(중국)`** (간체 아님). tx_sub는 언어추가 시 **전체이름으로 클릭** + 자막칸 **부분매칭 폴백**(수정 완료).
- **EN 영상 본언어가 한국어로 잡히면** → 한국어 행이 "동영상 언어"가 됨. set_video_lang로 영어 지정하거나, 있는 그대로 빈칸만 채움.

## ★★ 노출 4단계 파이프라인 (자막·제목설명·태그 다 된 뒤, 영상별) — W9 검증
> ⚠️ 이 단계 도구는 **CDP(디버그 크롬 9222)** 사용 → tx_sub/tx_meta로 크롬 죽였으면 **디버그 크롬 재기동**:
> `Start-Process chrome --remote-debugging-port=9222 --user-data-dir=assets\chrome_profile`
> ⚠️ playlist_add/card.py 등이 **거짓양성(추가=False·저장=OK)** 낼 수 있음 → **스크린샷으로 실제 반영 검증**.

**순서(태그 → 노출4 → 임베드):**
1. **태그** `tags_robust.py <VID> <tags.txt>` (500 근접·외국인우선, CDP).
2. **재생목록** `playlist_add.py <VID> "한국어 쉽게 배우기"` (CDP). 성공=처리=True·저장=OK.
3. **고정댓글** — ①게시=`post_and_pin.py <VID> <comment.txt>`(#contenteditable-root→#submit-button). ②**핀(고정)은 `pin_only.py <VID> "<HOOK부분문자열>"`** (재게시 금지, 이미 게시된 내 댓글만 고정).
   - ⚠️ **핵심**: 댓글 ⋮(더보기)는 **force 클릭 금지** → `#action-menu button`의 **bounding box 중심을 마우스 클릭**해야 팝업이 뜬다. 팝업 **첫 항목 = 📌 고정**(단어는 "고정", "맨 위에 고정" 아님) → 클릭 → 확인 다이얼로그 **"고정"** 버튼.
   - ⚠️ **댓글 1개면 과다 스크롤 금지**(댓글이 화면 밖으로 나감). 살짝(700) 후 `scroll_into_view`. 검증=재로드 후 "…님이 고정함" 배지.
   - stdout cp949라 "⋮" 인코딩 에러 → `PYTHONIOENCODING=utf-8`로 실행.
4. **최종화면(정확한 이전영상)** — 자동추천 endscreen.py 말고 **스크린샷 방식**: `/edit` 로드(9초 대기) → "엔딩 화면" 클릭 → **+ 요소 → 동영상 → "특정 동영상 선택"** → `내 동영상 검색`에 이전강의 제목 입력 → 결과 클릭 → **+ 요소 → 구독** → 저장. (W9: 이전=W8. KO=한국어 숫자·날짜·시간 / EN=Learn Korean Numbers, Dates)
5. **카드(특정영상)** — 스크린샷 방식: `/edit` → "카드" 클릭 → **동영상**(카드유형) → `내 동영상 검색` → 이전강의 클릭 → 저장. (W9: W8)
6. **웹 임베드**: drjayed.com 레슨 페이지에 유튜브 링크/임베드(R2 복사 금지).

**방법 철칙**: 자동도구 실패하면 **스크린샷 → 어디 누를지 좌표/텍스트 확인 → 다시 들어가기**. + 요소/특정 동영상 선택/내 동영상 검색 흐름은 최종화면·카드 동일.

**끝나면 DB 기록**: `youtube_uploads`(video_id·tags·notes에 자막/제목설명/태그/노출 상태) — 나중에 확인용. [[project-video-db-policy]]

## ❌ 쓰지 말 것
- **cap_robust.py (CDP)** — 갓 업로드된(처리중) 영상 + 오염된 페이지에서 자막칸 클릭이 편집기를 못 열어 **게시 실패**. 자막은 반드시 **tx_sub.py**.

---
관련 메모리: [[youtube-multilang-seo-pattern]] · 상세 다국어 패턴: `YOUTUBE_MULTILANG_PATTERN.md`
